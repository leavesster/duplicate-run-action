{
  "name": "duplicate-run-action",
  "version": "0.1.0",
  "description": "GitHub Action to detect and skip duplicate workflow runs by comparing git tree hashes",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --transpile-only",
    "test": "jest"
  },
  "keywords": [
    "github-action",
    "duplicate",
    "skip",
    "workflow"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}
